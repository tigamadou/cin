version: "3.8"
services:
  db:
    image: postgres:14
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-qrevent}
      POSTGRES_USER: ${POSTGRES_USER:-qreventuser}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-qreventpass}
    volumes:
      - db_data:/var/lib/postgresql/data
    restart: unless-stopped

  web:
    build: .
    command: gunicorn project.wsgi:application --bind 0.0.0.0:8000
    env_file: .env
    environment:
      - DATABASE_URL=postgres://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db:5432/${POSTGRES_DB}
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - ./media:/app/media
    depends_on:
      - db
    restart: unless-stopped

volumes:
  db_data:
